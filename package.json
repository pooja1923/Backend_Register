{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",  // Start the backend server
    "dev": "nodemon index.js", // Useful for development with automatic restarts
    "test": "echo \"Error: no test specified\" && exit 1", // Placeholder for tests
    "build": "echo \"No build step defined\" && exit 0" // Optional build step, remove if not needed
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.8.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22" // Automatically restart the server in development
  }
}
